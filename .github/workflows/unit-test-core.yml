name: 单元测试 - CoreService

on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      - name: 运行 CoreServiceTest
        run: mvn test -pl ruoyi-admin -Dtest="com.ruoyi.system.service.CoreServiceTest" -DfailIfNoTests=false -Dsurefire.failIfNoSpecifiedTests=false -am --no-transfer-progress
