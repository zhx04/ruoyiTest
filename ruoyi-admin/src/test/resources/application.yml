# CI 集成测试配置（覆盖主配置的连接信息）
# src/test/resources 优先级高于 src/main/resources，仅覆盖需要变更的属性
spring:
  profiles:
    active: druid
  datasource:
    druid:
      master:
        url: jdbc:mysql://127.0.0.1:3306/ry-vue?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
        username: root
        password: root123
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: ""
      database: 0
